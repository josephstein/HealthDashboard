import React from 'react'

export default class UserDetailsCompliances extends React.Component {
  render() {
    // const { userId } = this.props
    // if (!userId) { return }

    return (
      <div>
        Compliances
      </div>
    );
  }
}
