import React from 'react'

export default class UserDetailsPrescriptions extends React.Component {
  render() {
    // const { userId } = this.props
    // if (!userId) { return }

    return (
      <div>
        Prescriptions
      </div>
    );
  }
}
